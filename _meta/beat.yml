################### Mesosbeat Configuration Example #########################

############################# Mesosbeat ######################################

mesosbeat:
  # Defines how often an event is sent to the output
  period: 30

  # Defines the http port serviced
  # Default to 5050
  port: 5050

  # Defines the http host serviced
  # Default to localhost
  host: localhost
